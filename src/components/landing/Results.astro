---
import { TrendingUp, ShoppingBag, Award, HeartHandshake, CheckCircle } from '@lucide/astro';

const metrics = [
  {
    target: '30',
    prefix: 'PKR ',
    suffix: 'M+',
    exact: 'PKR 30M+',
    label: 'Revenue Supported',
    description: 'Total revenue supported across managed commercial campaigns and e-commerce stores.',
    blockClass: 'bg-[#ff762b] text-white',
    badgeClass: 'bg-white/20 text-white',
    Icon: TrendingUp,
  },
  {
    target: '29000',
    prefix: '',
    suffix: '+',
    exact: '29,000+',
    label: 'Product Sales',
    description: '29,000+ product sales supported across multiple product categories.',
    blockClass: 'bg-[#9a83f5] text-white',
    badgeClass: 'bg-white/20 text-white',
    Icon: ShoppingBag,
  },
  {
    target: '4.2',
    prefix: 'PKR ',
    suffix: 'M+',
    exact: 'PKR 4.2M+',
    label: 'Sales Supported',
    description: 'PKR 4.2M+ sales supported during the seasonal Wajib Livestock Qurbani campaign.',
    blockClass: 'bg-[#ddff35] text-[#061d33]',
    badgeClass: 'bg-[#061d33]/15 text-[#061d33]',
    Icon: Award,
  },
  {
    target: '150',
    prefix: '',
    suffix: '+',
    exact: 'More Than 150',
    label: 'Animals Sold',
    description: 'More than 150 animals sold through seasonal campaign execution.',
    blockClass: 'bg-[#ff514b] text-white',
    badgeClass: 'bg-white/20 text-white',
    Icon: HeartHandshake,
  },
];
---

<section id="results" class="py-20 lg:py-28 bg-[#061d33] text-white border-b border-white/10 overflow-hidden">
  <div class="mx-auto max-w-[1280px] px-6 sm:px-8">
    
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16 space-y-3">
      <span class="inline-flex items-center gap-2 text-xs font-medium font-mono uppercase tracking-[0.06em] text-[#ddff35]">
        <CheckCircle class="size-4 text-[#ddff35]" />
        <span>VERIFIED PERFORMANCE</span>
      </span>
      <h2 class="font-heading font-bold text-[28px] sm:text-[36px] lg:text-[44px] text-white leading-[1.12] tracking-[-0.025em]">
        Verified commercial results
      </h2>
      <p class="text-base sm:text-lg text-slate-300 font-body leading-[1.65] font-normal max-w-2xl mx-auto">
        Commercial milestones achieved across client accounts with 100% verified first-party metrics.
      </p>
    </div>

    <!-- 4 Large Colourful Metric Blocks -->
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
      {
        metrics.map((m, idx) => (
          <div
            class:list={[
              'reveal-on-scroll rounded-3xl p-8 lg:p-10 shadow-2xl flex flex-col justify-between transition-all duration-300 transform hover:-translate-y-2 group',
              m.blockClass
            ]}
            data-reveal-delay={idx * 100}
          >
            <div>
              <div class="flex items-center justify-between mb-8">
                <div class="size-14 rounded-2xl bg-current/15 flex items-center justify-center backdrop-blur-md">
                  <m.Icon class="size-7" />
                </div>
                <span class:list={['text-xs font-mono font-medium px-3 py-1 rounded-full uppercase tracking-[0.05em]', m.badgeClass]}>
                  VERIFIED
                </span>
              </div>

              <!-- Metric Value: Sora 700, 29-34px mobile, 34-42px desktop -->
              <div
                class="font-heading font-bold text-[29px] sm:text-[34px] lg:text-[42px] leading-none counter-value mb-4 tracking-[-0.025em]"
                data-counter-target={m.target}
                data-counter-prefix={m.prefix}
                data-counter-suffix={m.suffix}
                data-counter-exact={m.exact}
              >
                {m.exact}
              </div>

              <!-- Label: Inter 600, 14-16px, Natural title case -->
              <h3 class="font-body font-semibold text-[15px] sm:text-base leading-snug mb-2">
                {m.label}
              </h3>
              
              <!-- Description: Inter 400 leading 1.65 -->
              <p class="text-sm leading-[1.65] font-body font-normal opacity-90">
                {m.description}
              </p>
            </div>

            <div class="mt-8 pt-4 border-t border-current/20 flex items-center justify-between text-xs font-mono font-medium uppercase tracking-[0.05em]">
              <span>Performance Track</span>
              <span>100% Verified</span>
            </div>
          </div>
        ))
      }
    </div>

  </div>
</section>
