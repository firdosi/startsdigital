---
export interface Props {
  class?: string;
  as?: any;
}

const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag class={`mx-auto max-w-[1200px] w-full px-5 md:px-8 ${className}`}>
  <slot />
</Tag>
