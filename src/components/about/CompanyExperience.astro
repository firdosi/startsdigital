---
import SectionEyebrow from '../common/SectionEyebrow.astro';
import { site } from '../../site.config';
import { getProjectById } from '../../data/projects';
import { ArrowUpRight, TrendingUp, Award, Layers, ShieldCheck } from '@lucide/astro';

const bg = getProjectById('black-gold-fertilizer')!;
const wajib = getProjectById('wajib-livestock')!;
const rk = getProjectById('rk-reno-solutions')!;
const convort = getProjectById('convort-ai')!;

const projects = [
  {
    brand: bg.publicName,
    category: bg.sector,
    contribution: bg.shortSummary,
    result: bg.verifiedOutcome,
    link: `${site.basePath}${bg.caseStudyPath}`,
    image: `${site.basePath}/images/black-gold-official.png`,
    badgeColor: 'bg-[#ff762b]/10 text-[#ff762b] border-[#ff762b]/20',
    icon: TrendingUp
  },
  {
    brand: wajib.publicName,
    category: wajib.sector,
    contribution: wajib.shortSummary,
    result: wajib.verifiedOutcome,
    link: `${site.basePath}${wajib.caseStudyPath}`,
    image: null,
    badgeColor: 'bg-[#39c98a]/10 text-[#39c98a] border-[#39c98a]/20',
    icon: Award
  },
  {
    brand: rk.publicName,
    category: rk.sector,
    contribution: rk.shortSummary,
    result: null,
    link: `${site.basePath}${rk.caseStudyPath}`,
    image: `${site.basePath}/brands/rk-reno-solutions/screenshot.webp`,
    badgeColor: 'bg-sky-500/10 text-sky-600 border-sky-500/20',
    icon: Layers
  },
  {
    brand: convort.publicName,
    category: convort.sector,
    relationship: convort.relationshipType,
    contribution: convort.shortSummary,
    result: null,
    link: `${site.basePath}${convort.caseStudyPath}`,
    image: `${site.basePath}/brands/convort-ai/logo.webp`,
    badgeColor: 'bg-purple-500/10 text-purple-600 border-purple-500/20',
    icon: ShieldCheck
  }
];
---

<section id="experience" class="py-16 sm:py-20 lg:py-24 bg-[#061d33] text-white relative overflow-hidden border-b border-white/10 scroll-mt-44 sm:scroll-mt-40 lg:scroll-mt-36">
  <!-- Background Glow Accents -->
  <div class="absolute -right-20 -top-20 size-80 rounded-full bg-[#ff762b]/15 blur-3xl pointer-events-none"></div>
  <div class="absolute -left-20 -bottom-20 size-80 rounded-full bg-[#ddff35]/10 blur-3xl pointer-events-none"></div>

  <div class="mx-auto max-w-[1280px] px-6 sm:px-8 relative z-10">
    
    <!-- Section Header -->
    <div class="mb-12 text-center max-w-3xl mx-auto space-y-3">
      <SectionEyebrow label="WORK IN PRACTICE" align="center" motion={false} />
      <h2 class="font-heading font-bold text-[28px] sm:text-[36px] lg:text-[40px] text-white leading-tight tracking-[-0.025em]">
        The company story is reflected in the work.
      </h2>
      <p class="text-base sm:text-lg text-slate-200 font-body leading-relaxed font-normal">
        Selected projects show how Starts Digital has contributed across e-commerce, seasonal campaigns, local services and technology product development.
      </p>
    </div>

    <!-- 2x2 Proof Index Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-5xl mx-auto">
      {projects.map((p) => {
        const IconComp = p.icon;
        return (
          <div class="rounded-3xl border border-white/15 bg-white/5 backdrop-blur-md p-6 sm:p-8 flex flex-col justify-between shadow-xl group hover:border-[#ff762b]/40 transition-all duration-300">
            <div class="space-y-4">
              
              <!-- Header Row -->
              <div class="flex items-start justify-between gap-3 pb-4 border-b border-white/10">
                <div class="space-y-1">
                  <div class="flex items-center gap-2">
                    <span class={`inline-block px-2.5 py-0.5 rounded-full border text-[11px] font-mono font-semibold uppercase tracking-wider ${p.badgeColor}`}>
                      {p.category}
                    </span>
                  </div>
                  <h3 class="font-heading font-bold text-2xl text-white pt-1">{p.brand}</h3>
                  {p.relationship && (
                    <p class="font-mono text-xs text-slate-300">{p.relationship}</p>
                  )}
                </div>

                <a
                  href={p.link}
                  aria-label={`View ${p.brand} project details`}
                  class="size-10 rounded-full bg-white/10 text-white flex items-center justify-center group-hover:bg-[#ff762b] transition-colors shrink-0 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#ff762b]"
                >
                  <ArrowUpRight class="size-5 group-hover:translate-x-0.5 group-hover:-translate-y-0.5 transition-transform" />
                </a>
              </div>

              <!-- Media Preview or Typography Accent -->
              {p.image ? (
                <div class="overflow-hidden rounded-2xl border border-white/10 bg-slate-900/50 p-4 aspect-16/9 flex items-center justify-center">
                  <img
                    src={p.image}
                    alt={`${p.brand} showcase visual`}
                    width="400"
                    height="225"
                    loading="lazy"
                    decoding="async"
                    class="max-h-full max-w-full object-contain rounded-lg group-hover:scale-105 transition-transform duration-500"
                  />
                </div>
              ) : (
                <div class="rounded-2xl border border-white/10 bg-white/5 p-5 flex items-center gap-3">
                  <IconComp class="size-6 text-[#ddff35] shrink-0" />
                  <span class="text-xs font-mono text-slate-200">Verified Campaign Performance</span>
                </div>
              )}

              <!-- Contribution (>= 16px text-base) -->
              <div class="space-y-2">
                <span class="text-xs font-mono font-semibold uppercase tracking-wider text-slate-400 block">
                  Contribution
                </span>
                <p class="text-base text-slate-200 font-body leading-relaxed">
                  {p.contribution}
                </p>
              </div>

              <!-- Verified Result (Mandatory >= 16px text-base) -->
              {p.result && (
                <div class="pt-3 border-t border-white/10 space-y-1">
                  <span class="text-xs font-mono font-semibold uppercase tracking-wider text-[#ddff35] block">
                    Verified Outcome
                  </span>
                  <p class="text-base font-body font-semibold text-white leading-relaxed">
                    {p.result}
                  </p>
                </div>
              )}

            </div>

            <!-- Footer Link -->
            <div class="pt-4 mt-6 border-t border-white/10">
              <a
                href={p.link}
                class="inline-flex items-center gap-2 text-xs font-mono font-semibold text-[#ff762b] hover:text-white transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#ff762b]"
              >
                <span>EXPLORE PROJECT PROOF</span>
                <ArrowUpRight class="size-3.5" />
              </a>
            </div>

          </div>
        );
      })}
    </div>

  </div>
</section>
